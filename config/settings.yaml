# Consumer Duty Monitoring (Rgc15) runtime configuration.
#
# Loaded by `src/consumer_duty_monitoring/config.py`. Point at a different file with
# CONSUMERDUTY_SETTINGS; a path that is named but missing is a boot error, not
# a silent fall-back to built-in defaults.
#
# `${VAR}` and `${VAR:-default}` resolve in THREE states: unset takes the default written here,
# SET-AND-EMPTY REFUSES with ConfiguredEmptyError (an operator who emptied a value expressed an
# intent, and it names nothing, so it neither inherits this default nor quietly resolves to the
# empty string, which for a URL, an allowlist or a path is the permissive branch), and a set
# value wins. Unset the variable to take the default written here; do not blank it. This file holds env var NAMES and non-secret defaults
# only: no secret value is ever committed here.

# Data residency. Selected once and shared by the runtime and Terraform.
region: ${GCP_REGION:-asia-southeast1}

# Tenant partition asserted on outbound reviews when the verified principal carries none.
# (Built as one template expression: a literal "${" immediately before a template opener is
# ambiguous to render, so the whole reference is produced by the expression instead.)
tenant: ${CONSUMERDUTY_TENANT:-}

# Local profile stores. ":memory:" is the ephemeral, deterministic default the gate uses; a
# deployment points this at a durable path.
audit_path: ${CONSUMERDUTY_AUDIT_PATH:-:memory:}

# The external head anchor for the WORM audit chain (practices check C9). The hash chain cannot
# detect a TRUNCATED TAIL by itself: dropping the newest rows leaves a shorter chain that
# verifies perfectly. Point this at a file on a DIFFERENT volume, under different credentials,
# from audit_path, and every append also writes the chain head there. Empty (the default) means
# no anchor, which is correct for the ephemeral ":memory:" store and wrong for a durable one.
audit_anchor_path: ${CONSUMERDUTY_AUDIT_ANCHOR:-}

# The audience the managed (gcp) IAP identity adapter verifies the assertion AGAINST: the
# IAP-protected resource, "/projects/<NUM>/global/backendServices/<ID>" behind an HTTPS load
# balancer. It is per-deployment, so it is configuration and never a literal in the adapter.
# Three states, and UNSET and SET-AND-EMPTY land in the same place on purpose: both resolve to
# empty, and an empty audience REFUSES every caller rather than verifying without one.
# google-auth documents audience=None as "the audience is not verified", which accepts ANY
# Google-signed OIDC token, from any project or any app, and reads its email as a principal.
iap_audience: ${CONSUMERDUTY_IAP_AUDIENCE:-}

# Rule R8: where an escalated result is routed for human review. The managed profile REFUSES to
# swallow an escalation when this is empty, so a producer cannot ship with R8 unwired.
# The outbound S2S credentials are HUMAN_REVIEW_S2S_TOKEN / HUMAN_REVIEW_S2S_SIGNING_KEY (see .env.secrets.example),
# deliberately distinct from this service's own inbound CONSUMERDUTY_S2S_TOKEN.
review_url: ${HUMAN_REVIEW_URL:-}

# The outcome-test pack: the firm's Consumer Duty thresholds, as YAML a compliance officer can
# diff. Empty selects the shipped synthetic reference pack (rulepacks/consumer_duty.yaml).
outcome_pack_path: ${CONSUMERDUTY_OUTCOME_PACK:-}

# Local assessment store (SQLite). ":memory:" is the ephemeral, deterministic default the gate
# uses; a deployment points this at a durable path.
assessment_path: ${CONSUMERDUTY_ASSESSMENT_PATH:-:memory:}

# Where the offline warehouse export appends JSON Lines (empty means in-memory only), and the
# managed BigQuery destination (empty makes the managed export refuse rather than invent one).
warehouse_path: ${CONSUMERDUTY_WAREHOUSE_PATH:-}
warehouse_table: ${CONSUMERDUTY_WAREHOUSE_TABLE:-}

# The consent and preference store the managed vulnerable-customer test reads, read-only. Empty
# makes the managed consent lookup refuse rather than read an unknown preference as consent.
consent_url: ${CONSUMERDUTY_CONSENT_URL:-}

# The per-port adapter map: the ONLY place a binding lives, so switching the whole stack is a
# configuration change rather than a code edit. Every port must bind every profile; the loader
# refuses a partial block and `tests/test_settings_file.py` fails the build if this drifts from
# `config.DEFAULT_BINDINGS`.
project_id: ${GOOGLE_CLOUD_PROJECT:-}

adapters:
  assessment_store:
    local: consumer_duty_monitoring.adapters.local.assessment_store:LocalAssessmentStore
    gcp: consumer_duty_monitoring.adapters.gcp.assessment_store:FirestoreAssessmentStore
    onprem: consumer_duty_monitoring.adapters.onprem.assessment_store:OnPremAssessmentStore
  audit:
    local: consumer_duty_monitoring.adapters.local.audit:LocalAuditAdapter
    gcp: consumer_duty_monitoring.adapters.gcp.audit:CloudAuditAdapter
    onprem: consumer_duty_monitoring.adapters.onprem.audit:OnPremAuditAdapter
  consent:
    local: consumer_duty_monitoring.adapters.local.consent:LocalConsentLookup
    gcp: consumer_duty_monitoring.adapters.gcp.consent:ConsentStoreLookup
    onprem: consumer_duty_monitoring.adapters.onprem.consent:OnPremConsentLookup
  identity:
    local: consumer_duty_monitoring.adapters.local.identity:LocalIdentityAdapter
    gcp: consumer_duty_monitoring.adapters.gcp.identity:IapIdentityAdapter
    onprem: consumer_duty_monitoring.adapters.onprem.identity:OnPremIdentityAdapter
  narration:
    local: consumer_duty_monitoring.adapters.local.narration:LocalNarrator
    gcp: consumer_duty_monitoring.adapters.gcp.narration:GeminiNarrator
    onprem: consumer_duty_monitoring.adapters.onprem.narration:OnPremNarrator
  product_governance:
    local: consumer_duty_monitoring.adapters.local.product_governance:LocalProductGovernance
    gcp: consumer_duty_monitoring.adapters.gcp.product_governance:DocumentAiProductGovernance
    onprem: consumer_duty_monitoring.adapters.onprem.product_governance:OnPremProductGovernance
  review_router:
    local: consumer_duty_monitoring.adapters.local.review_router:LocalReviewRouter
    gcp: consumer_duty_monitoring.adapters.gcp.review_router:CloudReviewRouter
    onprem: consumer_duty_monitoring.adapters.onprem.review_router:OnPremReviewRouter

  # Tracing. Which BACKEND a deployed profile exports to is not a binding: the gcp
  # adapter reads OTEL_EXPORTER_OTLP_ENDPOINT and sends OTLP to the Hrz5 collector when it
  # is set, straight to Cloud Trace when it is not. Tracing is not essential to
  # correctness, so onprem is ABSENT rather than fatal, unlike the other placeholders.
  tracer:
    local: consumer_duty_monitoring.adapters.local.tracer:LocalNoopTracerAdapter
    gcp: consumer_duty_monitoring.adapters.gcp.tracer:CloudTracerAdapter
    onprem: consumer_duty_monitoring.adapters.onprem.tracer:OnPremTracerAdapter

  # The promotion authority (Hrz4). The local adapter scores offline but REFUSES to
  # promote: a promotion certified by a laptop with no quality service is certified by
  # nothing.
  evaluation:
    local: consumer_duty_monitoring.adapters.local.evaluation:LocalOfflineEvalAdapter
    gcp: consumer_duty_monitoring.adapters.gcp.evaluation:ManagedEvalGateAdapter
    onprem: consumer_duty_monitoring.adapters.onprem.evaluation:OnPremEvalAdapter
  signal_source:
    local: consumer_duty_monitoring.adapters.local.signal_source:LocalSignalSource
    gcp: consumer_duty_monitoring.adapters.gcp.signal_source:BigQuerySignalSource
    onprem: consumer_duty_monitoring.adapters.onprem.signal_source:OnPremSignalSource
  warehouse:
    local: consumer_duty_monitoring.adapters.local.warehouse:LocalWarehouseExport
    gcp: consumer_duty_monitoring.adapters.gcp.warehouse:BigQueryWarehouseExport
    onprem: consumer_duty_monitoring.adapters.onprem.warehouse:OnPremWarehouseExport
